<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:id="@+id/glowny_layout"
    tools:context="com.example.gabi.zadanie5v2.MainActivity">


    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lista telefonów"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listView"
        android:layout_below="@+id/textView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"/>

    <Button
        android:id="@+id/buttonShowCustomDialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Custom Dialog"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="165dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/buttonWyjscie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wyjscie"
        android:layout_marginTop="68dp"
        android:layout_alignTop="@+id/buttonShowCustomDialog"
        android:layout_toEndOf="@+id/textView" />

    <!--<Button-->
        <!--android:id="@+id/stopSound"-->
        <!--android:layout_width="60dp"-->
        <!--android:layout_height="50dp"-->
        <!--android:text="stop"-->
        <!--android:onClick="stopSound"-->
        <!--android:layout_below="@+id/playSound"-->
        <!--android:layout_alignEnd="@+id/buttonWyjscie"-->
        <!--android:layout_marginTop="59dp" />-->
    <Button
        android:id="@+id/idProstyDialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prosty Dialog"
        android:onClick="ProstyDialog"
        android:layout_alignBaseline="@+id/buttonShowCustomDialog"
        android:layout_alignBottom="@+id/buttonShowCustomDialog"
        android:layout_alignStart="@+id/stopSound" />

    <Button
        android:id="@+id/stopSound"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="stop sound"
        android:onClick="stopSound"
        android:layout_alignBottom="@+id/buttonWyjscie"
        android:layout_toEndOf="@+id/buttonWyjscie"
        android:layout_marginStart="26dp" />

</RelativeLayout>
